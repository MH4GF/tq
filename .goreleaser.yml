version: 2
builds:
  - main: .
    binary: tq
    goos: [darwin, linux]
    goarch: [amd64, arm64]
    ldflags: [-s -w]
archives:
  - formats: [tar.gz]
    format_overrides:
      - goos: darwin
        formats: [zip]
changelog:
  sort: asc
